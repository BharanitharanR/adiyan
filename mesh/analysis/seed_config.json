{
  "constants": {
    "decide_next_step_prompt_template": {
      "value": "Instruction: {instruction}\n\nFindings so far:\n{scratchpad}\n\nChoose your next action: call a tool to investigate further, or call finish once you have enough evidence.\n\nRules:\n- If the instruction refers to something ambiguously (\"it\", \"that\", \"the game\") and memory hasn't been checked yet, call recall_memory before asking for clarification.\n- search_documents only returns a filename, never content - read a document (read_document or search_within_document) before treating it as evidence or citing it.\n- For a specific question about an already-known document, use search_within_document, not read_document - read_document returns the whole document and truncates long ones before reaching the relevant part.\n- Use read_document only when you need the full text or a general overview.\n- Never state a specific real-world detail (an event, date, price, availability) unless a tool actually returned it.\n\n{grounding_rule}",
      "description": "Main ReAct decide-step prompt - how the model chooses between tools (document search, memory recall, agent consultation, database resolution) or finish. Needs {instruction}, {scratchpad}, and {grounding_rule} placeholders. Rewritten succinct - the original embedded a full narrative example (a tennis/recall_memory anecdote) directly in the text sent to the model on every single decide-step call, not just as documentation; the imperative rule alone carries the same instruction at a fraction of the token cost."
    },
    "compact_prompt_template": {
      "value": "Instruction: {instruction}\n\nCurrent scratchpad:\n{scratchpad}\n\nNew observation from {tool_name}:\n{observation}\n\nProduce the updated scratchpad.\n- Extract only what's genuinely relevant to the instruction, not raw text - a document being the closest match doesn't make its content relevant if it's actually about something else.\n- If nothing relevant, note that in open_questions instead of adding a finding.\n- Keep at most 8 findings, dropping the least relevant first.\n- Track documents_checked / agents_consulted when applicable.\n- Carry forward every still-relevant item from the current scratchpad - don't drop anything unless this observation supersedes it.",
      "description": "Folds one new tool observation into the running scratchpad. Needs {instruction}, {scratchpad}, {tool_name}, and {observation} placeholders. Rewritten succinct - same rules as before (8-finding cap, relevance-not-just-presence, carry-forward), the habit-formation-guide example moved out since the rule itself already states the distinction."
    },
    "final_answer_prompt_template": {
      "value": "Instruction: {instruction}\n\nEverything found:\n{scratchpad}\n\nYou're out of investigation steps - answer now.\n- For any specific, verifiable detail (an event, date, price, availability), use only what's in the scratchpad above; if it's not there, say you're unsure or leave it out.\n- A real but irrelevant finding is not an answer - if nothing found actually addresses the instruction, say so plainly.\n\n{grounding_rule}",
      "description": "Forced final-answer prompt, used when the ReAct loop hits its step cap without calling finish. Needs {instruction}, {scratchpad}, and {grounding_rule} placeholders."
    },
    "msg_no_matching_document": {
      "value": "No matching document found in the knowledge base.",
      "description": "Shown when search_documents finds no document confident enough to match a query."
    },
    "msg_document_not_found": {
      "value": "No document found with filename '{source_filename}'.",
      "description": "Shown when read_document is called with a filename that isn't in the knowledge base."
    },
    "msg_no_relevant_chunks": {
      "value": "No relevant passages found in '{source_filename}' for that question.",
      "description": "Shown when search_within_document finds nothing relevant enough inside an already-known document."
    },
    "msg_kb_empty": {
      "value": "The knowledge base is empty - no documents have been uploaded.",
      "description": "Shown when list_documents is called but nothing has ever been uploaded to the knowledge base."
    },
    "msg_no_contact": {
      "value": "No specific person is associated with this request - nothing to recall.",
      "description": "Shown when recall_memory is called but no contact_name was ever attached to this conversation."
    },
    "msg_nothing_in_memory": {
      "value": "Nothing relevant found in conversation memory.",
      "description": "Shown when recall_memory finds a contact but no past conversation snippets relevant to the query."
    },
    "msg_no_agents_discoverable": {
      "value": "No other agents are currently discoverable.",
      "description": "Shown when discover_agents finds no other agents registered in the mesh."
    },
    "msg_agent_not_registered": {
      "value": "No agent registered with id '{agent_id}' - call discover_agents first.",
      "description": "Shown when consult_agent is called with an agent_id that isn't actually registered in the mesh."
    },
    "msg_no_clear_answer": {
      "value": "I wasn't able to find a clear answer.",
      "description": "Fallback used if the ReAct loop's own final response has no text at all to fall back on."
    },
    "msg_unknown_tool": {
      "value": "Unknown tool: {tool_name}",
      "description": "Shown when the model calls a tool name that doesn't exist - should never happen in practice."
    },
    "strict_grounding": {
      "value": true,
      "description": "If true, only tool-verified evidence counts as a basis for an answer - even an ordinary general-knowledge question gets an honest \"nothing relevant found\" instead of an answer from the model's own training data. If false, general knowledge is a legitimate fallback when no document/memory/agent had anything relevant, but a specific verifiable detail (a date, a price, a port number) still requires real tool-verified evidence."
    },
    "observation_char_cap": {
      "value": 6000,
      "description": "Max characters kept from any single tool result before it's truncated - protects the ReAct loop's own prompt from being blown out by one huge document dump."
    },
    "doc_search_top_k": {
      "value": 5,
      "description": "How many passages search_within_document returns per query - more raises the odds the actually-relevant one is included, at the cost of a longer observation to read."
    }
  }
}
