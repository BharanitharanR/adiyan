pydantic>=2.0
python-dotenv>=1.0.0
pika>=1.3.0
requests>=2.31.0
flask>=3.0.0
flask-cors>=4.0.0
toml>=0.10.2
qrcode>=7.4.2
httpx>=0.27.0
langgraph>=1.0.0
langchain-core>=1.0.0
langchain-ollama>=1.0.0
langchain-mcp-adapters>=0.3.0
mcp>=1.20.0
duckduckgo-mcp-server>=0.5.0
llama-index-core>=0.14.0
llama-index-vector-stores-qdrant>=0.10.0
llama-index-embeddings-ollama>=0.9.0
qdrant-client>=1.9.0
docling>=2.75.0
mem0ai>=2.0.18
beanie>=2.2.0
pymongo>=4.9.0
croniter>=2.0.0
fpdf2>=2.7.0
# After installing, also run `crawl4ai-setup` once to download its Playwright
# browser binaries - pip install alone does not fetch them.
crawl4ai>=0.9.0
keyring>=25.0.0
workspace-mcp>=1.24.0

# AdiyanReader's local TTS (mesh/adiyan_reader/tts.py) - decodes Orpheus's
# audio-token output via SNAC; the Orpheus model itself is served through
# Ollama (legraphista/Orpheus:3b-ft-q4_k_m), not a separate inference stack.
# ffmpeg (a system binary, not pip-installable) is also required, for the
# WAV->Opus/OGG transcode WhatsApp's voice-note UI needs.
torch>=2.0.0
snac>=1.2.0

# Dev/test only - needed to run Tests/http/, not by the running app itself.
pytest>=8.0.0
